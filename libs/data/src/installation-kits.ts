// Flat add-on price charged once per project when a dealership opts into an
// installation kit at order time. One kit covers every inlay on the project, so
// the charge does not scale with inlay count. Keep this in sync with
// InstallationKitPriceCents in libs/data/pkg/pricing.go.
export const INSTALLATION_KIT_PRICE_CENTS = 9500;
