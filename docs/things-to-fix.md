When trying to type in the search bar on the catalog page, every keypress causes the entire page to rerender (which also unfocuses the textbox). The search box should be smooth to use, and its input should be debounced as the search param on the query. Look for other text fields that may have the same issue and fix them

Catalog doesn't use tags. Lets remove the tag search field and the tag comment on the catalog item card.

put dealerships name on pages/header as well as orders.

Put dealership name and internal reference on project

We need to make it so inlay updates are able to be created from the inlays page (kanban), not just the sinigle inlay page.

Way too many emails/notifications. Tone it down. Notifications should only be when cut, when shipped (to the dealership), and whenever there is action a internal user needs to take (for internal users, customized catalog items, custom items, chats, projects ordered).

pagination doesn't reset back to page 1 when the search changes in the catalog. This leaves people on blank pages.
